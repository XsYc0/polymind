# Shadow Evaluation

Shadow evaluation contracts and persistence tables are present, disabled by default. Shadow results do not alter user responses and should only run under explicit privacy and budget policies.
