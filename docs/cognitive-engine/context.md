# Context

The context engine preserves system messages, trims old irrelevant turns, constructs node-specific context, and can compact into a short summary when token estimates exceed limits. Security-reviewer context filters toward security, auth, config, code, and risk terms.
