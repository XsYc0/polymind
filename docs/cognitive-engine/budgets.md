# Budgets

Budgets enforce maximum wall-clock intent, provider calls, execution nodes, parallel calls, council participants, and optional estimated cost. Presets are economy, balanced, quality, local-only, and custom.

Plans that exceed node or parallel limits are rejected before execution.
