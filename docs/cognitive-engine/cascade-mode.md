# Cascade Mode

Cascade mode tries eligible models in bounded order and stops when deterministic evaluation passes or escalation is no longer allowed. It does not escalate blindly and is capped by provider-call and node budgets.
