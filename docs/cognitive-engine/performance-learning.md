# Performance Learning

Model performance observations are persisted from execution attempts and exposed through model/provider performance and leaderboard APIs. Metrics include sample count, success rate, latency percentiles, average latency, and fallback count.

Routing influence is intentionally bounded; cold starts still rely on configured model profiles.
