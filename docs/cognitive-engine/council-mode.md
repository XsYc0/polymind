# Council Mode

Council mode selects a bounded set of eligible candidates, obtains independent responses, and synthesizes one final answer. Participant count is conservative by default and budget-limited.

The deterministic judge/synthesis path is implemented. Dedicated judge-model routing is represented in the plan and remains policy-dependent.
