# Evaluation

The evaluation engine performs deterministic checks for non-empty output, JSON parsing when JSON is expected, code-oriented signals for code tasks, and keyword/requirement coverage. Results include pass/fail, score, threshold, reasons, defects, repair recommendation, and escalation permission.

Scores are heuristic signals, not objective truth.
