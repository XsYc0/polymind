# Cache

The semantic cache supports exact normalized request keys, privacy partitions, capability partitions, TTL, invalidation, stats, deterministic test embeddings, and private-output-safe CLI summaries. SQLite persistence tables are present; the in-memory cache is used for deterministic unit behavior.
