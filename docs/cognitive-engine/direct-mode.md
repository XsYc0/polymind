# Direct Mode

Direct mode selects one eligible model through the existing router, executes once, evaluates the response, records the plan, and returns one normalized completion. Routine low-risk tasks default to direct mode.
