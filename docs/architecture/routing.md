# Routing

Balanced score is:

`quality*0.45 + cost*0.2 + speed*0.2 + health*0.1 + localPreference*0.05 - recentFailurePenalty`

Costs are estimated only from configured model pricing. Quality is a configured profile value, not a semantic benchmark claim.
