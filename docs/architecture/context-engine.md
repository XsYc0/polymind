# Context Engine Architecture

The context engine estimates tokens, preserves system instructions, trims old context, and builds role-specific message windows. It is intentionally deterministic and does not require external embedding services.
