# Provider Contract

Providers implement identity, model listing, health checks, non-streaming chat, stream abstraction, normalized usage, normalized errors, declared capabilities, and abort/timeout support.

Task 1 adapters are mock, Ollama, and generic OpenAI-compatible.
