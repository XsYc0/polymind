# Cache Architecture

Cache keys combine normalized prompt content, model, privacy partition, response format, and required capabilities. Persistence tables record entries, lookups, and invalidations without exposing raw private prompts through default CLI output.
