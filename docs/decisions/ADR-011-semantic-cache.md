# ADR-011 Semantic Cache

Accepted.

Exact cache must work without embeddings. Semantic reuse is privacy-, capability-, schema-, validation-, TTL-, and configuration-aware. SQLite is the default persistence target.
