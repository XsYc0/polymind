# Virtual Models

Supported virtual models:

- `polymind/auto`
- `polymind/direct`
- `polymind/economy`
- `polymind/quality`
- `polymind/local`
- `polymind/council`
- `polymind/specialist`

Virtual models activate the cognitive engine for non-streaming requests and return normal OpenAI-compatible responses with optional `polymind` explanation metadata.
