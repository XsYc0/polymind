# Execution Policies

Requests may include a `polymind` extension with `mode`, `budgetPreset`, `maximumCost`, `maximumLatencyMs`, `privacy`, `explain`, `cache`, `ruflo`, and `omniroute` preferences. Unknown or unsupported fields are ignored by default validation.
