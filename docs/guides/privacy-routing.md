# Privacy Routing

Privacy policies can force local-only routing and prevent cache reuse across partitions. Private prompts are not exposed in cache CLI output by default, and request/response body retention remains disabled unless explicitly configured.
