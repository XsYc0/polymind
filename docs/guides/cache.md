# Cache Guide

Use `polymind cache stats`, `polymind cache purge --yes`, and `polymind cache inspect` for cache administration. Exact cache works without embeddings; semantic cache can use deterministic test embeddings or future pluggable providers.
