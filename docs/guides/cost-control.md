# Cost Control

Use budget presets and maximum call/node limits to constrain escalation, council size, and specialist planning. PolyMind estimates tokens from text length when provider tokenization is unavailable and records actual usage when providers return it.
